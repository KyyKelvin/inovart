"use client";import{FormEvent,useState}from"react";import{createClient}from"@/lib/supabase";
export function AdminLogin(){const[state,setState]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get("email"));setState("Solicitando link…");const{error}=await createClient().auth.signInWithOtp({email,options:{shouldCreateUser:false,emailRedirectTo:`${location.origin}/admin`}});setState(error?"Acesso não autorizado ou indisponível.":"Confira seu e-mail. O link é de uso único.")}return <form className="window" onSubmit={submit}><div className="window-bar">acesso_editorial.auth</div><div style={{padding:"2rem"}}><div className="field"><label htmlFor="email">E-mail autorizado</label><input id="email" name="email" type="email" required autoComplete="email"/></div><button className="button primary" style={{marginTop:"1rem"}}>Enviar link mágico →</button><p className="status" aria-live="polite">{state}</p><p style={{color:"var(--muted)",fontSize:13,lineHeight:1.6}}>Não há senha compartilhada. Apenas contas previamente autorizadas com papel editorial podem ler ou alterar dados internos.</p></div></form>}
