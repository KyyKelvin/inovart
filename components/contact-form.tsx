"use client";import{FormEvent,useState}from"react";import{createClient}from"@/lib/supabase";
export function ContactForm(){const[state,setState]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("Enviando…");const form=new FormData(e.currentTarget);const{error}=await createClient().from("contact_messages").insert({name:String(form.get("name")),email:String(form.get("email")),subject:String(form.get("subject")),message:String(form.get("message")),status:"new"});if(error)setState("Não foi possível enviar. Tente novamente.");else{e.currentTarget.reset();setState("Mensagem recebida.")}}return <form className="window" onSubmit={submit}><div className="window-bar">mensagem.txt</div><div className="form-grid" style={{padding:"2rem"}}><div className="field"><label htmlFor="name">Nome *</label><input id="name" name="name" required/></div><div className="field"><label htmlFor="email">E-mail *</label><input id="email" name="email" type="email" required/></div><div className="field full"><label htmlFor="subject">Assunto *</label><input id="subject" name="subject" required/></div><div className="field full"><label htmlFor="message">Mensagem *</label><textarea id="message" name="message" minLength={10} required/></div><div className="full"><button className="button primary">Enviar mensagem →</button> <output className="status" aria-live="polite">{state}</output></div></div></form>}
