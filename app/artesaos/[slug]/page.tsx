import Link from"next/link";import{Footer}from"@/components/footer";import{Window}from"@/components/window";import{demoArtisans}from"@/lib/content";
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=demoArtisans.find(x=>x.slug===slug)??demoArtisans[0];return <main id="conteudo"><section className="section grid-noise"><div className="site-shell"><span className="demo-note">Perfil demonstrativo — ainda não publicado no acervo</span><div className="section-head" style={{marginTop:"2rem"}}><div><p className="eyebrow">{a.code} / {a.place}</p><h1>{a.name}.</h1></div><div><p>{a.craft}</p><p>Este perfil reserva a estrutura editorial para a trajetória, os materiais, o território e os trabalhos do artesão. O texto real será criado após consentimento e revisão.</p><Link className="button" href="/artesaos">← Voltar ao arquivo</Link></div></div><Window label="retrato_autorizado.webp"><div className="card-visual" data-code="imagem pendente" style={{minHeight:"52vw",maxHeight:650}}/></Window></div></section><Footer/></main>}
